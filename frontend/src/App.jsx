import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'

function App() { //components always start with a capital letter
  return ( //needs to return one parent element, e.g. can't have two div's here... without different parent element.
    <div>
      <p>Hello World</p>
    </div>
  )
}

export default App
